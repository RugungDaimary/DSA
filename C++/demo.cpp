#include<bits/stdc++.h>
using namespace std;
int binarySearch(vector<pair<int,int>>&v,pair<int,int>&target){
    int n=v.size();
    int low=0;
    int high=n;                                                     
    int index=n;
    while(low<=high){
        int mid=(low+high)/2;
        if(times[mid][0])
    }

}
int main(){
    
    return 0;
}